# AI feature migration implementation plan

> Historical plan: cross-repository sharing has been replaced by the local `ai-model-capabilities/` module. The original showcase module is restored to JVM; no sibling checkout or external module path is required.

> **For agentic workers:** Use superpowers:subagent-driven-development for the independent shared-module task and its review; the coordinator implements the application integration alongside it.

**Goal:** Port image generation and image summaries with consistent UI and one shared KMP capability module.
**Architecture:** The original repository owns ai-model-capabilities. ShowcaseApp references it by configurable Gradle project path. Common Compose state and UI call a client-only application service; browser factories prevent AI execution.
**Tech Stack:** Kotlin Multiplatform, Compose, coroutines, serialization, Okio, existing storage and networking.
**Spec:** ../specs/2026-09-06-ai-port-design.md

## Global constraints
- New local branches only; no push; preserve original dirty checkouts.
- No copied capability module in ShowcaseApp.
- Preserve provider protocol validation, credential redaction, cancellation and bounded IO.
- AI buttons/settings appear on installed clients and remain absent on browser targets.
- Match original style, provider selection, generation and summary behavior.

## Task 1: Shared module
- [x] Convert original ai-model-capabilities build and source sets to KMP, preserving JVM compatibility where practical.
- [x] Move portable contracts/providers to commonMain and replace JVM-only IO, URL, time, crypto and transport seams.
- [x] Add repeatable byte image input and client transport injection usable from KMP clients.
- [x] Run JVM provider/client regression tests and KMP metadata/native compilation.
- [x] Commit shared-module changes and review them independently.

## Task 2: Application service and state
- [x] Inspect original provider configuration, styles, generation state and summary caching.
- [x] Reference shared module with configurable path and sibling default.
- [x] Port profile persistence, client service, generation task/results and summary cache with cancellation/retry handling.
- [x] Test profile/task state, source resolution, summary caching and web gating.

## Task 3: Compose integration
- [x] Port generator/provider settings/creation UI using existing Compose resources and theme.
- [x] Add native-only settings and source image actions.
- [x] Add per-mode summary switches and playback overlays for slide/fade/calendar.
- [x] Compile desktop, Android, iOS, JS and Wasm targets; run relevant tests.
- [x] Review full diff, fix actionable findings and commit locally without push.

## Validation outcome

- Application: 425 desktop tests passed; Android debug APK assembled; iOS device/simulator, JS and Wasm compilation passed.
- Shared module (`d7cce461`): 133 JVM tests passed; metadata, iOS device/simulator and native test compilation, JS and Wasm passed.
- Original Android caller compilation passed with only the missing local Firebase configuration task excluded.
- Independent application and shared-module reviews passed after regression-tested fixes.
- Actual paid inference and device photo-library export were not exercised. Original working copies remain separate; no push performed.
